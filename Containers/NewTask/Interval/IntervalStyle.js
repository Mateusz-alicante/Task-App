import { StyleSheet, Platform } from 'react-native';

export default StyleSheet.create({
    innerContainer: {
        display: "flex",
        flexDirection: "row"
    },
    label: {
        fontSize: 30,
        marginTop: 10
    },
    InputContainer: {
        width: "50%"
    }
    
  });
  